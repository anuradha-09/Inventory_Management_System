@using Inventory_Management_System.Models;
@model Product
@{
    ViewData["Title"] = "Edit";
}

<form asp-action="Edit" method="post" class="row g-3">
    <input type="hidden" asp-for="ProductId" />

    <div class="col-md-6">
        <label asp-for="Name" class="form-label"></label>
        <input asp-for="Name" class="form-control" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    <div class="col-md-2">
        <label asp-for="Quantity" class="form-label"></label>
        <input asp-for="Quantity" class="form-control" />
        <span asp-validation-for="Quantity" class="text-danger"></span>
    </div>

    <div class="col-md-2">
        <label asp-for="Price" class="form-label"></label>
        <input asp-for="Price" class="form-control" />
        <span asp-validation-for="Price" class="text-danger"></span>
    </div>

    <div class="col-md-2">
        <label asp-for="ReorderThreshold" class="form-label"></label>
        <input asp-for="ReorderThreshold" class="form-control" />
        <span asp-validation-for="ReorderThreshold" class="text-danger"></span>
    </div>

    <div class="col-md-6">
        <label asp-for="SupplierId" class="form-label">Supplier</label>
        <select asp-for="SupplierId" class="form-select" asp-items="@(new SelectList(ViewBag.Suppliers as IEnumerable<Supplier>, "SupplierId", "Name"))">
            <option value="">-- select supplier --</option>
        </select>
        <span asp-validation-for="SupplierId" class="text-danger"></span>
    </div>

    <div class="col-12">
        <button type="submit" class="btn btn-success">Save</button>
        <a asp-action="Index" class="btn btn-secondary">Cancel</a>
    </div>
</form>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
